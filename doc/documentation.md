# Documentation Tasks

## agent base and sample agent (get-time) functionnal diagram

Make a functional diagram of the base agent class `AgentBase`, and its derivated class `GetTimeAgent`, including the way it can be controlled through the `test-agent.js` file. Store the result in the file `doc/agent-functional-diagram.md`. Use `mermaid` syntax to create the diagrams. Use black ink in colored boxes.

